apiVersion: v1
kind: Secret
metadata:
  name: {{ include "spring-app.fullname" . }}-secret
  labels:
    {{- include "spring-app.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.secret }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
